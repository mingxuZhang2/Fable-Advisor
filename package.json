{
  "name": "fable-advisor",
  "version": "1.0.0",
  "description": "MCP server that exposes a third-party Fable endpoint as a read-only code advisor (headless Claude Code)",
  "type": "module",
  "bin": {
    "fable-advisor": "./server.js"
  },
  "scripts": {
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  }
}
